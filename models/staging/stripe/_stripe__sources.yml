version: 2

sources:
  - name: stripe
    database: raw
    schema: stripe
    tables:
      - name: payments
        identifier: payment
        description: Stripe payments source table
        columns:
          - name: payment_id
